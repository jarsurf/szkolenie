napis = "Ala <ma> kota"
licznik = 0
zliczaj = False


for litera in napis:
    if litera == "<":
    napis.index()

    if litera in napis == ">":
        j = litera.index()
    print(j - i)

for i, v in enumerate(napis):
    if litera == "<":
        min = i
    if v > cyf(max):