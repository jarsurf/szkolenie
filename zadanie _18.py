def zliczanie(n):

    count = 0
    s = 'ala ma kota a kot ma ale'
    for x in range(len(s)):
        if s[x:].startswith('a'):
        if s[x:].
            count += 1
            if count > n:
                print(count)

print(zliczanie(2))

